import ReactDOM from "react-dom/client";
import React from "react";
// import App from "./App";

const container = document.getElementById("root");
const root = ReactDOM.createRoot(container);
root.render(<h1>This is a test</h1>);